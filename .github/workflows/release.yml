name: Release

on:
  release:
    types: [published]

permissions:
  contents: write
  id-token: write

jobs:
  artifacts:
    uses: ./.github/workflows/artifacts.yml
    with:
      version: ${{ github.event.release.tag_name }}
    secrets: inherit
