on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  id-token: write

name: Release

jobs:
  artifacts:
    uses: ./.github/workflows/artifacts.yml
    with:
      version: ${{ github.ref_name }}
    secrets: inherit
